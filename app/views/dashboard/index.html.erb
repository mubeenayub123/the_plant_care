<div class="container">
  <div class="mt-5 d-flex justify-content-between">
    <div id="shartContainer"><%= @temprature_chart.render() %></div>
    <div id="humidityChartContainer"><%= @humidity_chart.render() %></div>
  </div>
  <div class="mt-5 d-flex justify-content-between">
    <div id="lightIntensityChartContainer"><%= @light_intensity_chart.render() %></div>
  </div>
  <div class="mt-5 d-flex justify-content-between">
    <% @moisture_charts.each do |moisture_chart| %>
    <div class="col-xs-6" id="<%= moisture_chart.renderAt -%>"><%= moisture_chart.render() %></div>
    <% end %>
  </div>
</div>
