<%= form_with(model: irrigation_system) do |form| %>
  <% if irrigation_system.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(irrigation_system.errors.count, "error") %> prohibited this irrigation_system from being saved:</h2>

      <ul>
        <% irrigation_system.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :uuid, style: "display: block" %>
    <%= form.text_field :uuid %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
